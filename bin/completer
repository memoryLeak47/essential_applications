#!/bin/bash
# completer v0

usage="usage:\tcompleter -a\n\tcompleter <binfile>"

if [ -z "$1" ]; then
	echo -e "$usage"
	exit
fi

if [ "$1" == "sc" ] || [ "$1" == "-a" ]; then
	complete -W "j jc jr c cc cr cpp cppc cppr" sc
fi

if [ "$1" == "cb" ] || [ "$1" == "-a" ]; then
	complete -W "$(cd $ul_tmpdir/cb; find)" cb 
fi

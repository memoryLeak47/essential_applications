#!/usr/bin/env python

import os
import sys

def err():
	print "Usage:\tfiletype <file>"
	sys.exit(0)

if len(sys.argv) == 2:
	f = os.popen('file -bi ' + sys.argv[1], 'r')
	if f.read().startswith('text'):
		print "text"
	else:
		print "binary"

else:
	err()

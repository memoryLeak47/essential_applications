#!/bin/bash
# version v2

req 'awk'

if [ -z "$1" ]; then
	echo "Usage: vrs <lb-script>"
	exit
elif [ ! -f "$1" ]; then
	echo "Can't find file $1"
	exit
fi

tmp=$(awk "NR==2 {print}" $1)
echo ${tmp##*v}

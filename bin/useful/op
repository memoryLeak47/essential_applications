#!/bin/bash

req "xargs"
req "$ul_editor"

tmp=$(find -name "$1")

for file in $tmp
do
	if [ $(filetype $file) == "text" ]; then
		results="$results $file"
	fi
done


numres=$(echo "$results" | wc -l)

$ul_editor $results

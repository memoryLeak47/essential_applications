#!/bin/bash

req "g++"

reset

if [ -z "$1" ]; then
	files=$(find -name "*.cpp" -type f)
else
	files=$(find -name "${1}.cpp")
	echo "only compiling $files"
fi

g++ -O2 -g -Wfatal-errors -c $files -std=c++11
g++ *.o -o exe -std=c++11

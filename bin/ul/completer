#!/bin/bash

usage="Usage:\tcompleter -a\n\tcompleter <binfile>"

if [ -z "$1" ]; then
	echo -e "$usage"
	exit
fi

if [ "$1" == "sc" ] || [ "$1" == "-a" ]; then
	complete -W "j jc jr c cc cr cpp cppc cppr" sc
fi

if [ "$1" == "cb" ] || [ "$1" == "-a" ]; then
	tmp=$(cd $ul_localdir/cb; find -wholename "./*" | sed 's/^.\///g')
	res=
	for ok in $tmp
	do
		if [ -d $ul_localdir/cb/$ok ]; then
			res="$res ${ok}/"
		else
			res="$res $ok"
		fi
	done
	complete -W "$res" cb 
fi

if [ "$1" == "goto" ] || [ "$1" == "-a" ]; then
	tmp=$(cat $ul_localresdir/gotolib)
	res=
	for ok in $tmp
	do
		res="$res ${ok%:*}"
	done
	complete -W "$res" goto
fi
